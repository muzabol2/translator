export type Dictionary = {
   companyName: string,
   components: {
      header: {
         title: string,
      },
      footer: {
         flatIcon: string,
         libreTranslate: string,
      }
   }
}
